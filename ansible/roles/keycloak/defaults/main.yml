---
base_url: mydomain.com

keycloak:
  db_host: postgres
  db_name: keycloak
  db_user: postgres
  admin_user: admin
  working_dir: "/etc/keycloak"
  realm_name: MyRealm
  url: "auth.{{ base_url }}"
