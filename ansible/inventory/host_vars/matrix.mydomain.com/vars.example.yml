# The bare domain name which represents your Matrix identity.
# Matrix user ids for your server will be of the form (`@user:<matrix-domain>`).
#
# Note: this playbook does not touch the server referenced here.
# Installation happens on another server ("matrix.<matrix-domain>").
#
# If you've deployed using the wrong domain, you'll have to run the Uninstalling step,
# because you can't change the Domain after deployment.
#
# Example value: example.com
matrix_domain: mydomain.com

# This is something which is provided to Let's Encrypt when retrieving SSL certificates for domains.
#
# In case SSL renewal fails at some point, you'll also get an email notification there.
#
# If you decide to use another method for managing SSL certifites (different than the default Let's Encrypt),
# you won't be required to define this variable (see `docs/configuring-playbook-ssl-certificates.md`).
#
# Example value: someone@example.com
matrix_ssl_lets_encrypt_support_email: 'mail@mydomain.com'

# A shared secret (between Coturn and Synapse) used for authentication.
# You can put any string here, but generating a strong one is preferred (e.g. `pwgen -s 64 1`).
matrix_coturn_turn_static_auth_secret: '9dlf7bjHso6n2mGhB0di3kdnChangeMe'

# A secret used to protect access keys issued by the server.
# You can put any string here, but generating a strong one is preferred (e.g. `pwgen -s 64 1`).
matrix_synapse_macaroon_secret_key: '90sloOfs03N7293HhbmIXQ2oChangeMe'

# A Postgres password to use for the superuser Postgres user (called `matrix` by default).
#
# The playbook creates additional Postgres users and databases (one for each enabled service)
# using this superuser account.
matrix_postgres_connection_password: '9Sdk7ns3K922Mcsd2ChangeMe'

matrix_synapse_enable_registration: false

matrix_nginx_proxy_enabled: false
matrix_nginx_proxy_proxy_matrix_client_api_forwarded_location_synapse_oidc_api_enabled: true

matrix_nginx_proxy_proxy_matrix_nginx_status_enabled: true

matrix_synapse_configuration_extension_yaml: |
  oidc_providers:
    - idp_id: keycloak
      idp_name: "Keycloak"
      issuer: "https://auth.mydomain.com/auth/realms/IMIS"
      client_id: "synapse"
      client_secret: "fd9b7d16-a2e3-4d8d-99af-b6bChangeMe"
      scopes: ["openid", "profile"]
      user_mapping_provider:
        config:
          localpart_template: "{% raw %}{{ user.preferred_username }}{% endraw %}"
          display_name_template: "{% raw %}{{ user.name }}{% endraw %}"


matrix_jitsi_enabled: true
 
matrix_jitsi_jicofo_auth_password: agebdcc6aa6d882c6e974da8ChangeMe
matrix_jitsi_jvb_auth_password: 4sc3d4badd697a3b5efd2d36ChangeMe
matrix_jitsi_jibri_recorder_password: 7ef64cdc3616f230f886119bChangeMe
matrix_jitsi_jibri_xmpp_password: 14cbb837cd76cad1c387b7ccChangeMe


base_url: mydomain.com
install_ssl_certificates: true
ssl_email: contact@mydomain.com

base:
  enabled: true

nextcloud:
  enabled: false

keycloak:
  enabled: true
  url: "auth.{{ base_url }}"
  internal_ip: "172.22.0.2"
  internal_port: 8443
  db_host: postgres
  db_name: keycloak
  db_user: postgres
  db_password: 9Sdk7ns3K922Mcsd2ChangeMe
  admin_user: test
  admin_password: 8Sjn6HBDo228sH2h1jChangeMe
  realm_name: IMIS
  client_secret: fd9b7d16-a2e3-4d8d-99af-b6bChangeMe
